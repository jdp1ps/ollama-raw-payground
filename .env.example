# Copy to .env and set your own code before distributing.
ACCESS_CODE=change-me

# raw=true: prompt fed verbatim, no chat template (default).
# Set to false to let Ollama apply each model's chat template.
RAW=true
